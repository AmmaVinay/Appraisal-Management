package com.team4.appraisalApp;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * The AppraisalAppApplication class is the entry point for the Spring Boot application.
 */
@SpringBootApplication
public class AppraisalAppApplication {

	/**
	 * The main method serves as the entry point for the Spring Boot application.
	 * @param args command-line arguments.
	 */
	public static void main(String[] args) {
		SpringApplication.run(AppraisalAppApplication.class, args);
	}
}
